<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Devenir Sponsor</title>
</head>
<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
    <div class="container mx-auto px-4 py-12">
        <div class="max-w-2xl mx-auto">
            <div class="bg-white rounded-2xl shadow-xl p-8">
                <h1 class="text-3xl font-bold text-center text-gray-800 mb-8">
                    Devenez Sponsor
                </h1>
                
                <form class="space-y-6" action="/Sponsoring/updte" method="POST" >
                    <!-- Logo Upload Section -->
                    <input type="hidden" name="sponsor_id" value="{{ sponsor.id }}">
                    <div class="space-y-4">
                        <label class="block text-gray-700 text-lg font-medium mb-2">
                            Logo de votre entreprise
                        </label>
                        <div class="relative">
                            <div class="border-2 border-dashed border-gray-300 rounded-lg p-8 text-center hover:border-blue-500 transition-colors cursor-pointer bg-gray-50">
                                <input type="file" name="logo"value="{{sponsor.logo}}"  accept="image/*" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer" />
                                <div class="space-y-2">
                                    <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
                                        <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                                    </svg>
                                    <div class="text-gray-600">
                                        Glissez votre logo ici ou
                                        <span class="text-blue-500 hover:text-blue-600">parcourez vos fichiers</span>
                                    </div>
                                    <p class="text-sm text-gray-500">PNG, JPG jusqu'à 5MB</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Name Input Section -->
                    <div class="space-y-4">
                        <label class="block text-gray-700 text-lg font-medium mb-2">
                            Nom de votre entreprise
                        </label>
                        <input 
                        name="nameSponser"
                        value="{{sponsor.name}}"
                            type="text" 
                            class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors text-gray-900 bg-white placeholder-gray-400"
                            placeholder="Entrez le nom de votre entreprise"
                        />
                    </div>

                    <!-- Submit Button -->
                    <button 
                        type="submit" 
                        class="w-full bg-blue-600 text-white rounded-lg px-6 py-3 text-lg font-medium hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transform transition-all duration-200 hover:scale-[1.02]"
                    >
                       Mettre à jour le sponsor
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
</html>